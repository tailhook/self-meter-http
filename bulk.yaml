minimum-bulk: v0.4.5

versions:

- file: Cargo.toml
  block-start: ^\[package\]
  block-end: ^\[.*\]
  regex: ^version\s*=\s*"(\S+)"

- file: frontend/webpack.config.js
  regex: VERSION:\s*JSON\.stringify\("v([^"]+)"\)
